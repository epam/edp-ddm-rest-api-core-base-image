apiVersion: v1
kind: Service
metadata:
  name: {{ include "rest-api-core-base-image.fullname" . }}
  labels:
    {{- include "rest-api-core-base-image.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rest-api-core-base-image.selectorLabels" . | nindent 4 }}
