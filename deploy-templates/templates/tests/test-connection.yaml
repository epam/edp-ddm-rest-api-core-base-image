apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rest-api-core-base-image.fullname" . }}-test-connection"
  labels:
    {{- include "rest-api-core-base-image.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rest-api-core-base-image.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
